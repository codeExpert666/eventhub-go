# Java-Go Parity Matrix

本文记录 Go 版 EventHub 与 Java 版 EventHub 在业务语义、接口契约、错误码、数据库模型、测试策略和文档沉淀方式上的对齐状态。

Java 版参考项目：

```text
/Users/xinnz/Library/Mobile Documents/com~apple~CloudDocs/Code/Java/eventhub
```

最近核验日期：2026-06-03。

本矩阵只做对齐索引，不替代设计文档、implementation note 或 ADR。详细背景统一放在 `docs/ai/design/`、`docs/ai/implementation/` 和 `docs/ai/adr/`。

## 状态说明

- `已对齐`：当前 Go 代码、文档或测试已经能支撑本阶段对应语义。
- `已决策`：Go 版已经通过设计文档或 ADR 固化方向，但当前阶段可能尚未完成全部代码落地。
- `规则已初始化`：Go 版已写入约束，但尚未有业务代码可验证。
- `待迁移`：Java 版已有能力，Go 版尚未实现。
- `待决策`：Go 版需要 ADR 或设计文档明确技术选择。
- `不适用`：Java 版实现细节不直接迁移到 Go。

## 对齐矩阵

| 领域 | Java 版来源 | Go 版目标 | 当前状态 | 说明 |
| --- | --- | --- | --- | --- |
| 协作与文档基线 | `AGENTS.md`、`.agents/skills/backend-design-first/SKILL.md`、`docs/templates/*`、Java `docs/ai/*` | `AGENTS.md`、`.agents/skills/backend-design-first/SKILL.md`、`docs/templates/*`、`docs/ai/*`、`.codex/config.toml` | 已对齐 | Go 版已建立设计优先、implementation note、ADR、parity matrix、质量门禁和固定总结结构；本次审计见 `docs/ai/design/006-parity-matrix-audit.md`、`docs/ai/implementation/006-parity-matrix-audit.md`。 |
| 工程入口与 HTTP 运行基线 | `backend/src/main/java/com/eventhub/EventhubApplication.java`、Spring Boot Web、`backend/pom.xml` | `go.mod`、`cmd/eventhub/main.go`、`internal/app`、`internal/http/server.go`、`internal/http/router.go`、`Makefile` | 已对齐 | Go 版使用标准库 HTTP server + chi，不迁移 Spring/Maven 容器模型；已具备最小启动、路由、优雅关闭、`go test`/`go vet`/Makefile 基线。`Bootstrap` / `Run` 分别包装启动装配错误和 HTTP server 非预期错误，`main` 统一记录 `app.Run()` 返回的未处理错误后再退出，避免主错误静默丢失或重复打印；这是 Go 进程边界实现，不改变 Java API 契约。参考 ADR-0002 和 design/implementation 011。 |
| 分层与 package layout | Java `controller / service / mapper / entity / config / security` | Go `handler -> service -> repository -> sqlc/database`，以及 `internal/app`、`internal/platform`、`internal/security` 规则 | 已决策 | 当前已落地 `internal/app`、`internal/http/handler/system`、`internal/http/dto/system`、`internal/service/system`、`internal/platform/{clock,idgen,log}`；未开始的 `domain/repository/security` 不创建空 Go package。参考 design-002/005、ADR-0005。 |
| HTTP handler、DTO 与 service contract 边界 | Java Controller、`dto/request`、`vo`、Service 类 | `internal/http/handler/<module>`、`internal/http/dto/<module>`、`internal/service/<domain>/{service,command,query,result,usecase}.go` | 已对齐 | system 样板已落地；Go 版不设置 `internal/http/vo`，HTTP DTO 不进入 service，service Command/Result 不带 JSON tag。参考 design-003/004/005、ADR-0005/0006。 |
| Go 内部依赖组织与装配边界 | Java Spring DI、Controller 依赖 Service、Service 依赖 Mapper/Repository 的分层语义 | `AGENTS.md`、`.agents/skills/backend-design-first/SKILL.md`、`internal/app/application.go`、`internal/app/bootstrap.go`、`internal/app/providers`、`internal/http/router.go`、`internal/http/server.go`、`internal/http/handler/{auth,user}`、`internal/http/middleware/auth.go`、`internal/service/auth`、`internal/platform/db` | 已对齐 | Go 端已收敛过度接口化和 router option 模式：handler 直接依赖具体 service，repository interface 保留为持久化边界，事务 runner 归属 `platform/db`，router 只注册路由。2026-06-03 已把 composition root 运行时代码落地：`Application` 负责应用级资源释放，`Bootstrap(ctx context.Context)` 只表达主装配流程，细节拆入 `internal/app/providers`；`Bootstrap` 为 provider 启动失败补充装配阶段上下文，`main` 记录未处理错误后退出；`authsvc.Dependencies` 已删除，auth service 改为显式构造参数，带依赖字段的 auth middleware 改为结构体 + `NewAuth`。该变化仅影响 Go 内部组织，不改变 Java API、错误码、JWT、refresh token hash 或 DB 契约。参考 design/implementation 009、010 和 011。 |
| 统一响应、错误码与异常映射 | `common/api/ApiResponse.java`、`ErrorCode.java`、`BusinessException.java`、`GlobalExceptionHandler.java` | `internal/http/response`、`internal/apperror`、`internal/http/validation`、`internal/http/middleware/recover.go` | 已对齐 | 字段保持 `code/message/data/requestId/timestamp`；已初始化 `COMMON-000/400/401/404/500` 和 `AUTH-401/403/409`；JSON/字段校验映射 `COMMON-400`，未预期 panic 映射 `COMMON-500`。auth 专属失败流程仍随 auth 模块迁移。 |
| requestId、配置与结构化日志 | `infra/logging/RequestIdFilter.java`、`application*.yml`、Logback MDC、`AuthTokenProperties.java` | `internal/platform/idgen`、`internal/http/middleware/request_id.go`、`internal/config`、`internal/platform/log`、`internal/platform/db`、`internal/platform/redis`、`configs/*.env.example` | 已对齐 | 合法 `X-Request-Id` 复用，非法或缺失时重建；写入响应头、context、统一响应体和 `slog` 字段。Go 当前覆盖 app/env/port/version/log，并已新增 MySQL、Redis 和 auth token 配置；app 在配置 MySQL DSN 时装配 auth repository/service/middleware。参考 `docs/ai/design/008-auth-register-login-access-token.md`、`docs/ai/implementation/008-auth-register-login-access-token.md`。 |
| system 与 actuator API | `modules/system/controller/SystemController.java`、`SystemService.java`、`EchoRequest.java`、`PingInfo.java`、`EchoInfo.java`、Actuator 配置与测试 | `internal/http/handler/system`、`internal/http/dto/system`、`internal/service/system`、`internal/http/router.go` | 已对齐 | 已实现 `GET /api/v1/system/ping`、`POST /api/v1/system/echo`、`GET/HEAD /actuator/health`、`GET/HEAD /actuator/info`。Go 暂无 Spring Security 默认认证链，未支持方法当前由 router 映射为 `COMMON-404`，待 auth/security 阶段再对齐 Java 的 `AUTH-401` 场景。 |
| 分页契约 | `common/api/PageRequest.java`、`PageResponse.java` | `internal/page` | 已对齐 | 保持 1-based page、默认 1/20、最大 100、offset、`totalPages`、`hasNext`、`hasPrevious` 和 items 浅拷贝语义。 |
| 当前基础测试与质量门禁 | Java `ApiResponseTest`、`PageRequestTest`、`PageResponseTest`、`BusinessExceptionTest`、`SystemControllerTest` | Go `internal/http/*_test.go`、`internal/apperror/error_test.go`、`internal/page/page_test.go`、`internal/platform/idgen/request_id_test.go`、`.golangci.yml` | 已对齐 | 当前 Go foundation 已用 `httptest` 和单元测试覆盖统一响应、错误码、requestId、system、actuator、recover、分页；`gofmt/go test/go vet/make test/make vet` 可运行，lint 配置已存在但工具安装依赖环境。 |
| OpenAPI / Swagger | `infra/openapi/OpenApiConfig.java`、Controller `@Operation/@Tag`、`OpenApiProductionSecurityTest`、Springdoc 配置 | `api/openapi/.gitkeep`、未来 `api/openapi/eventhub.yaml` | 待迁移 | Go 版目前只有 OpenAPI 落点，没有契约文件、生成代码或 validate 命令；后续 API 稳定后迁移 Java 路径、schema、prod hardening 与验证流程。 |
| 数据库迁移与持久化边界 | Flyway `V1__init_backend_foundation.sql`、`V2__stage_1_auth_jwt_rbac.sql`、`V3__create_auth_sessions.sql`、`MyBatisConfig.java`、`mapper/auth/*.xml`、entity | `migrations/000001_system_bootstrap.*.sql`、`migrations/000002_auth_schema.*.sql`、`sqlc.yaml`、`internal/repository`、`internal/repository/mysql/{queries,sqlc}`、`internal/platform/db` | 已对齐 | Go 已建立 golang-migrate + sqlc + `database/sql` 持久化底座，表、字段、唯一约束、外键、索引、状态字符串和 seed 与 Java 当前 schema 对齐；Go 将 Java V2/V3 合并为 `000002_auth_schema`，属于从空库起步的刻意差异；`OpenMySQL` 强制 `parseTime=true` 保证 TIMESTAMP 到 `time.Time` 的扫描契约；RoleMapper 列表查询无匹配时返回空 slice，对齐 MyBatis 空 List 语义。参考 `docs/ai/design/007-persistence-foundation.md`、`docs/ai/implementation/007-persistence-foundation.md`、ADR-0007、ADR-0008、ADR-0010。 |
| 数据库测试策略 | Java `application-test.yml`、H2 profile、mapper tests、auth session concurrency tests | `internal/repository/mysql/mysql_repository_integration_test.go`、`internal/platform/db/errors_test.go` | 已对齐 | Go 刻意不采用 H2，改用 Testcontainers MySQL 验证 migration up/down、seed、MySQL unique constraint、repository 事务上下文和 auth session 条件更新；Docker/provider 不可用时测试会 skip。参考 ADR-0009。 |
| Redis 认证一致性边界 | Java `application*.yml` Redis 配置、Actuator redis health 配置、auth session MySQL 权威记录 | `internal/platform/redis`、`docs/ai/design/007-persistence-foundation.md` | 已决策 | Go 已新增 Redis client 底座，但 Redis 暂不参与认证强一致；refresh token、logout、用户禁用后的权威状态仍以 MySQL `users` / `auth_sessions` 为准，后续缓存或 denylist 需另行设计。 |
| auth 注册和登录 API | `modules/auth/controller/AuthController.java`、`RegisterRequest`、`LoginRequest`、`LoginResponse`、`AuthServiceImpl.java`、`TokenServiceImpl.java`、`AuthIntegrationTest.java` | `internal/http/handler/auth`、`internal/http/dto/auth`、`internal/service/auth`、`internal/security/{password,jwt,refresh}`、`internal/http/auth_integration_test.go` | 已对齐 | Go 已实现 `POST /api/v1/auth/register` 和 `POST /api/v1/auth/login`；注册默认绑定 `USER`，重复账号映射 `AUTH-409`，登录返回 access token、refresh token、authorizationScheme、过期秒数、sessionId 和用户摘要；登录成功创建 `ACTIVE` auth session，refresh token 明文只返回一次且 DB 只保存 `sha256:<hex>`。参考 `docs/ai/parity/java-auth-api-contract.md`、design/implementation 008、ADR-0011/0012。 |
| auth refresh 与 logout API | `AuthController.refresh/logout`、`RefreshTokenRequest`、`TokenPairResponse`、`AuthServiceImpl.refresh/logout`、refresh token rotation tests | 未来 `internal/http/handler/auth`、`internal/http/dto/auth`、`internal/service/auth`、`internal/security/refresh` | 待迁移 | Go 本次只在登录响应中返回 refresh token，并保存 hash；尚未实现 `POST /api/v1/auth/refresh`、refresh token 轮换、重放检测、`POST /api/v1/auth/logout` 或 access token denylist。 |
| 当前用户 API | `UserController.java`、`UserInfo.java`、`SecurityConfig`、`AuthIntegrationTest.meShouldReturnCurrentUserWithValidToken` | `internal/http/handler/user`、`internal/http/dto/user`、`internal/service/user`、`internal/http/middleware/auth.go` | 已对齐 | Go 已实现 `GET /api/v1/me`；认证 middleware 解析 Bearer access token 后按 `sub` 回 DB 加载最新用户状态和角色，禁用用户旧 token 返回 `AUTH-401`，响应不暴露 passwordHash。参考 design/implementation 008、ADR-0013。 |
| 管理员用户 API | `AdminUserController.java`、`AdminUserQueryRequest`、`UpdateUserStatusRequest`、`UserInfo`、admin integration tests | 未来 `internal/http/handler/user` 或 `admin`、`internal/http/dto/user`、`internal/service/user` | 待迁移 | Go 尚未实现 `GET /api/v1/admin/users`、`PATCH /api/v1/admin/users/{userId}/status`、用户分页筛选、状态切换或管理员 RBAC。 |
| JWT、认证上下文与 RBAC 安全边界 | `SecurityConfig.java`、`JwtClaims.java`、`JwtCodec.java`、`JwtAuthenticationFilter.java`、`AuthenticatedPrincipal*`、`SecurityUtils.java`、auth/security ADRs | `internal/security/{jwt,principal}`、`internal/http/middleware/auth.go`、`internal/service/user/principal.go` | 已对齐 | Go 已实现 access token 签发和解析，claim 仅包含 `iss/sub/iat/exp/jti/sid/typ=access`；不写角色、邮箱、用户名、用户状态；认证 middleware 每次回库加载用户状态和角色，并把 `security.Principal` 写入 context。RBAC 管理端规则待管理员 API 阶段迁移。参考 ADR-0011/0013。 |
| auth session 持久化底座 | `AuthSessionEntity.java`、`AuthSessionStatus.java`、`AuthSessionMapper.java`、`AuthSessionMapper.xml`、`V3__create_auth_sessions.sql`、`AuthSessionMapperTest.java`、`AuthSessionConcurrencyTest.java` | `migrations/000002_auth_schema.*.sql`、`internal/repository/auth_session_repository.go`、`internal/repository/mysql/auth_session_repository.go`、`internal/repository/mysql/queries/auth_sessions.sql`、`internal/repository/mysql/mysql_repository_integration_test.go` | 已对齐 | Go 已实现 `auth_sessions` 表、ACTIVE/REVOKED 状态、refresh token hash 唯一约束、session_id 唯一约束、按 session/hash 查询、last_seen、revoke、status update 和 old hash + old version 条件轮换；refresh token 生成/解析和 auth service/API 仍待迁移。参考 design/implementation 007。 |
| refresh token 与 auth session 服务流程 | `AuthTokenProperties.java`、`AuthSessionService*`、`RefreshTokenHasher.java`、`RefreshTokenParser.java`、`AuthServiceImpl.java`、auth integration tests | `internal/security/refresh`、`internal/service/auth`、`internal/http/handler/auth`、`internal/http/dto/auth` | 已决策 | Go 已实现登录时生成 opaque refresh token、保存 `sha256:<hex>` hash、创建 ACTIVE auth session，并返回 Java 对齐的登录 token pair；refresh endpoint、轮换、重放检测和 logout 仍待迁移。参考 ADR-0012、design/implementation 008。 |
| 认证错误与安全响应 | `AuthException.java`、`RestAuthenticationEntryPoint.java`、`RestAccessDeniedHandler.java`、`SecurityErrorResponseWriter.java` | `internal/apperror`、`internal/http/handler/auth`、`internal/http/middleware/auth.go` | 已对齐 | `AUTH-401/403/409` 码值已初始化并接入注册、登录和 `/me`：重复账号、账号或密码错误、用户禁用、缺失/过期/篡改 token、禁用用户旧 token 均返回 Java 对齐的错误码和核心消息；管理员 RBAC `AUTH-403` 场景待管理端 API 阶段迁移。 |
| 容器化与部署配置 | Java `backend/Dockerfile`、`docker-compose.yml`、`application-dev/prod/test.yml` | Go `configs/*.env.example`，未来 Dockerfile/compose/deploy config | 待迁移 | Go 当前只有环境变量示例和本地运行命令，没有容器镜像、compose、数据库服务编排或 prod profile 等价配置。 |
| 活动、票务、订单、支付、通知、审计 | Java `docs/roadmap/stage-2` 至 `stage-7`，当前 production code 尚未落地 | Go 后续 `event/order/payment/notification/audit` 相关 domain/service/repository/handler | 待决策 | 双端当前 production code 都未进入这些业务模块；Go 后续应以 Java roadmap 和未来 Java 实现为语义来源，重点关注库存扣减、幂等、订单状态机、支付回调、通知和操作日志。 |

## 后续维护规则

1. 每迁移一个 Java 业务模块，必须新增或更新对应矩阵行。
2. 如果 Go 版刻意偏离 Java 版实现方式，但保持业务语义一致，需要在设计文档中说明。
3. 如果 Go 版无法保持接口或错误码兼容，必须新增 ADR 或在设计文档中写明理由。
4. 矩阵只记录对齐状态和索引，详细设计仍放在 `docs/ai/design/`、`docs/ai/implementation/` 和 `docs/ai/adr/`。
