# EventHub OpenAPI 契约的 oapi-codegen 配置。
# 控制生成文件中的 Go package 名，与输出目录 api/openapi/gen 保持一致。
package: gen

generate:
  # CLI `-generate types` 在配置文件中对应 `models`。
  models: true
  chi-server: true

output: api/openapi/gen/eventhub.gen.go
